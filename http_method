 go 获取http Get/Post 请求参数
 
 http Get/Post/Delete/Put
  Get 查询
  Post 提交(更新，非幂等)
  Delete 删除
  Put 提交(幂等：提交多次，执行效果相同，例如：发送两个帖子，如果产生两条记录->非幂等，只产生一条记录->幂等)
 
 func MyHandler(w http.ResponseWriter, r *http.Request) {}
 
 Get：
  get 通过 url 后缀形式传递参数： /get?key=test
  r.FormValue("key")
  
 Post:
  post 通过 body 传递参数，并通过Content-type标记 body 传递形式
  Content-type: application/json ({"key": "test"})
    通过 ioutil.ReadAll(r.Body) 读取body内容
  Content-type: application/x-www-form-urlencode (key=test)
    通过 r.PostFormValue("key") 获取
